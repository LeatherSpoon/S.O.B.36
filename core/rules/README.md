# Rules extension point

Phase 0's explicitly original test policy lives in `core/actions/action_service.gd`.
Verified edition rules will replace that policy behind the same action interface.
No commercial rules have been inferred or implemented here.
